const someFunctions = () => {
  const capitalize = (string) => {
    // Capitalize it
    const capitalizedString = "";
    return capitalizedString;
  };

  return { capitalize };
};

export default someFunctions;
